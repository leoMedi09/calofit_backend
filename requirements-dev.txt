# Desarrollo y CI local. Instalar después del entorno base:
#   pip install -r requirements.txt
#   pip install -r requirements-dev.txt
#
# Lint rápido:
#   ruff check app scripts tests
#   ruff check app scripts tests --fix   # corrige lo automático (imports)

-r requirements.txt

ruff>=0.8.0,<1
pytest>=8.0,<9
pytest-asyncio>=0.24,<1
